import headerStyle from "./Header.module.css";

const Header = () => {
  return (
    <div className={`${headerStyle.title} text-white`}>
      GALATASARAY QUESTİONS AND ANSWER
    </div>
  );
};

export default Header;
